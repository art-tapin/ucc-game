using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OpeningTextTrigger : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GameObject.Find("ww").GetComponent<TextTrigger>().enabled = false;
    }

    // Update is called once per frame
    void Update()
    {
        // 
        //if (GameObject.Find( == false) {
        //    Debug.Log("end timelapse");
        //} 
       
    }

    void StartText()
    {
        //GameObject.Find("ww").GetComponent<TextTrigger>().enabled = true;
    }
}
